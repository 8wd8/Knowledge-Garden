name: code-sync

on:
  workflow_run:
    workflows: mkdocs-deploy
    types: completed

jobs:
  push-to-mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Clone
        run: |
          git config --global init.defaultBranch main
          git init
          git remote add origin https://shenweiyan:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
          git fetch --all
          for branch in `git branch -a | grep remotes | grep -v HEAD`; do
            git branch --track ${branch##*/} $branch
          done
        env:
          GITHUB_REPOSITORY: shenweiyan/Knowledge-Garden
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          
      - name: Push to Jihulab
        run: |
          remote_repo="https://shenweiyan:${JH_PASSWORD}@jihulab.com/${JH_REPOSITORY}.git"
          git remote add jihulab "${remote_repo}"
          # useful for debugging
          git show-ref 
          git branch --verbose
          # publish all
          git push --all --force jihulab
          git push --tags --force jihulab
        env:
          JH_REPOSITORY: shenweiyan/Knowledge-Garden
          JH_PASSWORD: ${{ secrets.JIHU_PASSWORD }}
          
      - name: Push to Gitee Knowledge-Garden
        run: |
          remote_repo="https://shenweiyan:${GITEE_PASSWORD}@gitee.com/${GITEE_REPOSITORY}.git"
          git remote add gitee "${remote_repo}"
          git show-ref
          git branch --verbose
          # publish all
          git push --all --force gitee
          git push --tags --force gitee
        env:
          GITEE_REPOSITORY: shenweiyan/Knowledge-Garden
          GITEE_PASSWORD: ${{ secrets.GITEE_PASSWORD }}

      - name: Push to Gitee shenweiyan
        run: |
          remote_repo="https://shenweiyan:${GITEE_PASSWORD}@gitee.com/${GITEE_REPOSITORY}.git"
          git remote add gitee "${remote_repo}"
          git show-ref
          git branch --verbose
          # publish all
          git push --all --force gitee
          git push --tags --force gitee
        env:
          GITEE_REPOSITORY: shenweiyan/shenweiyan
          GITEE_PASSWORD: ${{ secrets.GITEE_PASSWORD }}
